apiVersion: apps/v1
kind: Deployment
metadata:
  name: earscope-model
spec:
  replicas: 1 # override to dev after base 1 replicas
  template:
    spec:
      containers:
        - name: earscope-model
          image: 103.155.246.50/earscope-model:dev